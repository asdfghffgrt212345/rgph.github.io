# Резервный способ передачи проекта
Если GitHub недоступен, проект можно передать в виде ZIP-архива.
В ZIP должны находиться папки `notebooks`, `data` и `docs`, а также файл `README.md`.
Перед отправкой нужно распаковать архив в отдельную папку и проверить, что файлы открываются.
